var checkPad = setInterval(function(){
    let characterTop = parseInt(window.getComputedStyle(jugador).getPropertyValue("top"));
    let blockLeft = parseInt(window.getComputedStyle(pad).getPropertyValue("left"));
    if(blockLeft<100 && blockLeft>-100 && characterTop>=130){

    }
});